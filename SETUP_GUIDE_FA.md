# راهنمای کامل راه‌اندازی پروژه - از صفر تا صد

## 📋 پیش‌نیازها (نرم‌افزارهای مورد نیاز)

### 1. نصب نرم‌افزارهای پایه

#### Windows:
1. **Python 3.11+**
   - دانلود از: https://www.python.org/downloads/
   - هنگام نصب، گزینه "Add Python to PATH" را فعال کنید

2. **PostgreSQL 15+**
   - دانلود از: https://www.postgresql.org/download/windows/
   - پسورد پیش‌فرض: `postgres` (یا خودتان تنظیم کنید)

3. **Android Studio**
   - دانلود از: https://developer.android.com/studio
   - برای ساخت و اجرای اپلیکیشن اندروید

4. **Git** (اختیاری)
   - دانلود از: https://git-scm.com/downloads

---

## 🚀 مراحل راه‌اندازی (به ترتیب)

### مرحله 1: راه‌اندازی دیتابیس PostgreSQL

#### 1.1. نصب و راه‌اندازی PostgreSQL

```bash
# اگر PostgreSQL نصب است، سرویس را شروع کنید
# از Services در Windows یا pgAdmin استفاده کنید
```

#### 1.2. ایجاد دیتابیس

```sql
-- با pgAdmin یا psql اجرا کنید:
CREATE DATABASE inventory_db;
```

یا از Command Line:
```bash
psql -U postgres
CREATE DATABASE inventory_db;
\q
```

---

### مرحله 2: راه‌اندازی Backend (FastAPI)

#### 2.1. باز کردن Terminal/PowerShell

در مسیر پروژه:
```bash
cd C:\Users\Farasoo\Desktop\inventory
```

#### 2.2. ایجاد Virtual Environment

```bash
# ایجاد محیط مجازی
python -m venv venv

# فعال‌سازی محیط مجازی
venv\Scripts\activate
```

#### 2.3. نصب Dependencies

```bash
cd backend
pip install -r requirements.txt
```

#### 2.4. ایجاد فایل .env

در پوشه `backend` فایل `.env` ایجاد کنید:

```env
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/inventory_db
STORAGE_TYPE=local
LOCAL_STORAGE_PATH=storage/images
MODEL_PATH=models/yolov8_inventory.onnx
```

**نکته:** اگر پسورد PostgreSQL شما متفاوت است، آن را تغییر دهید.

#### 2.5. ایجاد پوشه‌های لازم

```bash
# در پوشه backend
mkdir storage
mkdir storage\images
mkdir models
```

#### 2.6. اجرای Backend

```bash
# از پوشه backend
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**✅ اگر موفق بود، باید ببینید:**
```
INFO:     Uvicorn running on http://0.0.0.0:8000
INFO:     Application startup complete.
```

#### 2.7. تست Backend

مرورگر را باز کنید و به آدرس بروید:
- API: http://localhost:8000
- مستندات API: http://localhost:8000/docs
- Health Check: http://localhost:8000/health

---

### مرحله 3: راه‌اندازی Android App

#### 3.1. باز کردن Android Studio

1. Android Studio را باز کنید
2. گزینه **"Open"** را انتخاب کنید
3. پوشه `android` را انتخاب کنید (نه `android/app`)

#### 3.2. Sync Gradle

- Android Studio به صورت خودکار Gradle را sync می‌کند
- اگر نشد، روی **"Sync Now"** کلیک کنید
- صبر کنید تا dependencies دانلود شوند (ممکن است چند دقیقه طول بکشد)

#### 3.3. تنظیم API Endpoint

فایل را باز کنید:
```
android/app/src/main/java/com/inventory/app/InventoryRepository.kt
```

خط 23 را پیدا کنید و آدرس را تنظیم کنید:

**برای Emulator (شبیه‌ساز):**
```kotlin
private val baseUrl = "http://10.0.2.2:8000/"
```

**برای دستگاه واقعی:**
```kotlin
// IP کامپیوتر خود را پیدا کنید:
// Windows: ipconfig در CMD
// سپس:
private val baseUrl = "http://192.168.1.XXX:8000/"  // IP خود را بگذارید
```

#### 3.4. ساخت و اجرای App

1. یک **Emulator** ایجاد کنید یا **دستگاه واقعی** را وصل کنید
2. روی دکمه **Run (▶)** کلیک کنید
3. App را انتخاب کنید و **OK** کنید

**✅ اگر موفق بود:**
- App روی دستگاه/Emulator نصب و اجرا می‌شود
- صفحه اصلی با دوربین نمایش داده می‌شود

---

### مرحله 4: تست سیستم

#### 4.1. تست Backend

در مرورگر:
```
http://localhost:8000/docs
```

1. روی **POST /api/v1/images/upload** کلیک کنید
2. **Try it out** را بزنید
3. یک تصویر انتخاب کنید
4. **Execute** کنید
5. باید پاسخ با detections را ببینید

#### 4.2. تست Android App

1. App را باز کنید
2. دکمه **Capture** را بزنید
3. تصویر گرفته می‌شود
4. تصویر به Backend ارسال می‌شود
5. نتایج نمایش داده می‌شود

#### 4.3. مشاهده Analytics

1. در App، دکمه **View Analytics** را بزنید
2. باید جداول و چارت‌ها را ببینید

#### 4.4. مشاهده Recommendations

1. در App، دکمه **View Recommendations** را بزنید
2. باید توصیه‌های هفتگی را ببینید

---

## 🔄 ترتیب اجرا (خلاصه)

```
1. PostgreSQL → راه‌اندازی و ایجاد دیتابیس
   ↓
2. Backend → نصب dependencies و اجرا
   ↓
3. Android Studio → باز کردن پروژه
   ↓
4. تنظیم API endpoint در Android
   ↓
5. اجرای Android App
   ↓
6. تست کامل سیستم
```

---

## ⚠️ مشکلات رایج و راه حل

### مشکل 1: Backend اجرا نمی‌شود

**راه حل:**
```bash
# بررسی کنید PostgreSQL در حال اجرا است
# بررسی کنید port 8000 آزاد است
# بررسی کنید .env فایل درست است
```

### مشکل 2: Android App نمی‌تواند به Backend وصل شود

**راه حل:**
- اگر از Emulator استفاده می‌کنید: `http://10.0.2.2:8000/`
- اگر از دستگاه واقعی استفاده می‌کنید:
  - IP کامپیوتر را پیدا کنید
  - Firewall را غیرفعال کنید یا port 8000 را باز کنید
  - مطمئن شوید Backend در حال اجرا است

### مشکل 3: Gradle Sync خطا می‌دهد

**راه حل:**
- اینترنت را بررسی کنید
- در Android Studio: **File → Invalidate Caches → Restart**

### مشکل 4: دیتابیس خطا می‌دهد

**راه حل:**
```bash
# بررسی کنید PostgreSQL در حال اجرا است
# بررسی کنید DATABASE_URL در .env درست است
# دیتابیس را دوباره ایجاد کنید
```

---

## 📱 تست نهایی

### چک‌لیست:

- [ ] PostgreSQL در حال اجرا است
- [ ] Backend روی http://localhost:8000 اجرا می‌شود
- [ ] API Docs در http://localhost:8000/docs باز می‌شود
- [ ] Android App نصب و اجرا می‌شود
- [ ] دوربین در App کار می‌کند
- [ ] تصویر گرفته می‌شود
- [ ] تصویر به Backend ارسال می‌شود
- [ ] Analytics نمایش داده می‌شود
- [ ] Recommendations نمایش داده می‌شود

---

## 🎯 دستورات سریع

### راه‌اندازی Backend:
```bash
cd backend
venv\Scripts\activate
uvicorn app.main:app --reload
```

---

## 📞 پشتیبانی

اگر مشکلی پیش آمد:
1. Logs را بررسی کنید
2. مستندات را بخوانید
3. مشکلات رایج را چک کنید

**موفق باشید! 🚀**


